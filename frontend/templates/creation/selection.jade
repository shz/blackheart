.selection
  .selection-header

  .instructions
    h2 Choose the image that best represents
    h2.name= name
    h2 Before time runs out

  h1.name= name

  .pieces
    a.flip-hex(href='#')
      .back
        img(src='/frontend/image/icons/' + name.toLowerCase() + '1.png')
        .red
      .front
    a.flip-hex(href='#')
      .back
        img(src='/frontend/image/icons/' + name.toLowerCase() + '2.png')
        .red
      .front
    a.flip-hex(href='#')
      .back
        img(src='/frontend/image/icons/' + name.toLowerCase() + '3.png')
        .red
      .front
    a.flip-hex(href='#')
      .back
        img(src='/frontend/image/icons/' + name.toLowerCase() + '4.png')
        .red
      .front
    a.flip-hex(href='#')
      .back
        img(src='/frontend/image/icons/' + name.toLowerCase() + '5.png')
        .red
      .front
    a.flip-hex(href='#')
      .back
        img(src='/frontend/image/icons/' + name.toLowerCase() + '6.png')
        .red
      .front

  .counter
    .s1
    .s2
    .s3
    .s4
    .s5
    .s6
